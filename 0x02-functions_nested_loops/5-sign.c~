#include "holberton.h"

/**
*print_sign - print sign of a number
*@n: parametrer
*Return: 1 for +, 0 for 0, -1 for -
*/

int print_sign(int n)
{

  int ac = n;

if (ac > 48)
{
_putchar('+');
return (1);
}
else if (ac == 48)
{
_putchar('0');
return (0);
}
else
{
_putchar('-');
return (-1);
}
}
